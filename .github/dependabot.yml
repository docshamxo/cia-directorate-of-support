# === FILE HEADER ===
# Title: Dependabot
# Path: .github/dependabot.yml
# Created: 2026-07-14
# Created by: docshamxo
# Modified:
#   - 2026-07-14 | docshamxo | Add CI, Dependabot, and repository validation tooling.
#   - 2026-07-14 | docshamxo | Add required file headers and footers across the repository.
#   - 2026-07-14 | docshamxo | Refresh file header modification logs after banner rollout.
#   - 2026-07-14 | docshamxo | Fix misleading CI badge and harden README presentation. (#7)
#   - 2026-07-15 | docshamxo | Add Google Drive links to unit staff documents. (#10)
#   - 2026-07-17 | docshamxo | Group updates; keep Actions SHA pins reviewable.
#   - 2026-07-17 | docshamxo | Commit prefixes; include pytest-cov and pre-commit in pip-dev.
# === END FILE HEADER ===

version: 2
updates:
  - package-ecosystem: pip
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    labels:
      - dependencies
      - python
    commit-message:
      prefix: "deps(pip)"
    groups:
      pip-runtime:
        patterns:
          - "discord.py"
          - "python-dotenv"
          - "PyYAML"
          - "requests"
      pip-dev:
        patterns:
          - "ruff"
          - "pytest"
          - "pytest-*"
          - "pip-audit"
          - "pre-commit"

  - package-ecosystem: pip
    directory: /requirements
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    labels:
      - dependencies
      - python
    commit-message:
      prefix: "deps(pip)"
    groups:
      pip-runtime:
        patterns:
          - "discord.py"
          - "python-dotenv"
          - "PyYAML"
          - "requests"
      pip-dev:
        patterns:
          - "ruff"
          - "pytest"
          - "pytest-*"
          - "pip-audit"
          - "pre-commit"

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    labels:
      - dependencies
      - github-actions
    commit-message:
      prefix: "deps(actions)"
    # Prefer reviewing SHA-pinned Action bumps as a single PR when possible.
    groups:
      github-actions:
        patterns:
          - "*"

# === FILE FOOTER ===
# End of file: .github/dependabot.yml
# Maintained by: docshamxo
# === END FILE FOOTER ===
